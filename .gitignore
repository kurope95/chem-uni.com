# ------------------------------------------------------------------
# Studijní podklady třetích stran — NEPATŘÍ do veřejného repozitáře
# (autorsky chráněné knihy; jeden soubor navíc přesahuje limit GitHubu)
# ------------------------------------------------------------------
*.pdf

# Snímky zadání od vyučující — osobní podklad, na web ani do repozitáře nepatří
*.jpg
*.jpeg
*.png

# Klíč se správnými odpověďmi ke všem otázkám — repozitář je veřejný,
# takže tohle by si studenti mohli otevřít. Zůstává jen lokálně.
kontrola-otazek/

# Sestavená složka k nasazení — vzniká z ostatních souborů (make_dist.py)
web/

# Lokální stav nástrojů
.wrangler/
node_modules/
.claude/

# Zálohy a dočasné soubory
*.bak
*.tmp
*~

# Pomocné skripty pro kontrolu, které se nenasazují
/qa.js
/overlap.js

# ------------------------------------------------------------------
# Sestavovací nástroje (tools/) jsou v repozitáři, ale tohle z nich nesmí:
# ------------------------------------------------------------------
# data tiskového klíče — obsahují správné odpovědi ke všem otázkám
pool.json
qa_payload.json
# naskenované stránky učebnice a digesty z ní — autorsky chráněný text
kniha/
DIGEST.md
# zadání pro agenty — doslovné citace soukromé komunikace
BRIEF*.md
# mezisoubory Pythonu
__pycache__/
*.pyc
